# Vector

Мой реализация контейнера `std::vector<T>`, поддерживающая большинство его возможностей. 
Покрыта простейшими unit-тестами в `test.cpp`.

Убедиться в корректности можно выполнив 
```console
mkdir build
cd build
cmake ..
cmake --build .
./MyVector
```
